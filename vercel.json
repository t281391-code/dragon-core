{
  "regions": ["sin1"],
  "buildCommand": "npx prisma migrate resolve --rolled-back 20260507000100_add_shift_participants_and_overtime || true && npx prisma migrate deploy && npx prisma db seed && npm run build"
}
